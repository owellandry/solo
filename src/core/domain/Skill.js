export class Skill {
  constructor({ id, provider, category, title, description, icon }) {
    this.id = id;
    this.provider = provider;
    this.category = category;
    this.title = title;
    this.description = description;
    this.icon = icon;
  }
}
